// ⚠️ Note: We set "signAndEditExecutable": false to skip Windows code signing.
// Normally, apps are signed with a certificate to avoid "unknown publisher" warnings.
// Since we don’t have a signing certificate, signing is disabled so the build works.
